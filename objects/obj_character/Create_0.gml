image_speed = 0;
image_index = 0;

/*
function test_menu_appears() {
  test_describe("menu object should appear on screen", function() {
	  test_it("menu object are present", function() {
			assert_exists(obj_character);
			assert_exists(obj_leaderboard);
			assert_exists(obj_mode);
			assert_exists(obj_quit);
		});
  });
}

test_menu_appears();
test_run_all();
*/
