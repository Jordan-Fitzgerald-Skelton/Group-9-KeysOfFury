image_speed = 0;
image_index = 0;
/*
function test_assert() {
	test_describe("object should back to its original colour", function() {
		test_it("users mouse is no longer above this object. it has canged back to its original colour", function() {
			assert(image_speed == 0);
			assert(image_index == 0);
		});
	});
}
 test_assert();
 test_run_all();
 */
 