image_speed = 0;
image_index = 1;

/*
function test_assert() {
	test_describe("object should change colour", function() {
		test_it("users mouse is above this object. it has canged colour", function() {
			assert(image_speed == 0);
			assert(image_index == 1);
		});
	});
}
 test_assert();
 test_run_all();
 */
 